import styled from "styled-components";

export const RegisterGrid = styled.div`
	display: grid;
	row-gap: 10px;
`;

export const Form = styled.form`
	display: grid;
	row-gap: 12px;
`;

export const MessageNormal = styled.span`
	font-size: 14px;
	color: var(--color-black);
	font-weight: 300;
	margin: .5em 0;
	text-align: center;
`;