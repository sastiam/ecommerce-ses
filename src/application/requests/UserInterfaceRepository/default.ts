import { IUserInterface } from "../../metadata/interfaces/UI";

const UIDefaultContent : IUserInterface = {
	bannerTitle : 'Título por Defecto',
	bannerDescription : 'Esta pequeña descripcion es para cuando el servidor este caido...',
	bannerVideo : ''
}

export {
	UIDefaultContent
}